// Escreva um algoritmo que armazene o ano atual e o ano de
// nascimento de uma pessoa. Escrever uma mensagem no console
// que diga se ela poderá ou não votar este ano (não é necessário
// considerar o mês em que a pessoa nasceu).

// Armazena o número inteiro positivo em uma variável
let numero = Number(prompt("Digite um número: "));

// Verifica se o número é par ou ímpar
if (numero % 2 === 0) {
  // Se for par, exibe uma mensagem indicando isso
  alert("Número é par!");
} else {
  // Se for ímpar, exibe uma mensagem indicando isso
  alert("Número é ímpar!");
}
